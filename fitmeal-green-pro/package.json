{
  "name": "fitmeal-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "FitMeal full-stack demo with Express, PostgreSQL and VNPAY integration",
  "type": "module",
  "scripts": {
    "dev": "node --watch server/index.js",
    "start": "node server/index.js",
    "db:migrate": "node server/scripts/migrate.js",
    "db:seed": "node server/scripts/seed.js",
    "check": "node --check server/index.js && node --check server/db.js && node --check server/config.js && node --check server/vnpay.js && node --check server/scripts/migrate.js && node --check server/scripts/seed.js && node --check app.js && node --check assistant.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0",
    "pg": "^8.16.3",
    "zod": "^4.1.5"
  }
}
